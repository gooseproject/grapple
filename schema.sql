CREATE TABLE grapple (
       id INT AUTO_INCREMENT,
       package TEXT,
       branch TEXT,
       commit_id TEXT,
       author TEXT,
       email TEXT,
       status INT,
       primary key(id)
);